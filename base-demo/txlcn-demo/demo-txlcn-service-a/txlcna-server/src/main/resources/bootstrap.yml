server:
  port: 20011

spring:
  application:
    name: demo-txlcn-service-a

  profiles:
    active: dev,common

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

    sentinel:
      log:
        dir: ./logs/csp/
      transport:
        dashboard: localhost:10009