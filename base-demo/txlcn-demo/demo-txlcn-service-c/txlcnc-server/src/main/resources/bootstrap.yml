server:
  port: 20013

spring:
  application:
    name: demo-txlcn-service-c

  profiles:
    active: dev,common

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

    sentinel:
      log:
        dir: ./logs/csp/
      transport:
        dashboard: localhost:10009